
<div class='container'>
  <h1>Decks</h1>
  <p>Choose a deck to start a game, or create a <a href='/newdeck'>new deck</a>.</p>
  <ul>
   <% @decks.each do |deck|%>
   <li><div class="deck_name"><%= deck.name %> - <%= deck.description %> - </div><div class="deck_selection"><a href="/decks/<%=deck.id%>">select.</a></div></li>
   <%end%>
  </ul>
</div>
